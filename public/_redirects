# -------------------------------------------------------------------
# 🌐 PIXEL PALACE: SPA ROUTING RULES (NETLIFY/STATIC HOSTS)
# -------------------------------------------------------------------

# 1. ASSET PASS-THROUGH
# Ensure real files (images, fonts, JS chunks) are served directly.
/assets/* /assets/:splat  200

# 2. SPA CATCH-ALL
# Send all other navigation requests to index.html so React Router takes over.
/* /index.html  200
